<Page
  x:Class="Spacium.Views.Pages.DashboardPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:Spacium.Views.Pages"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  Title="DashboardPage"
  d:DataContext="{d:DesignInstance local:DashboardPage,
                                     IsDesignTimeCreatable=False}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
  ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  mc:Ignorable="d"
>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header Card with Gradient Background -->
        <Border Grid.Row="0" Grid.RowSpan="3" Background="{DynamicResource ApplicationBackgroundBrush}" Margin="20,20,20,20" CornerRadius="16" Padding="40" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Source="pack://application:,,,/Assets/logo.png" Width="140" Height="120" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                <TextBlock FontSize="32" FontWeight="Bold" Text="Bienvenue sur Spacium!"
                           HorizontalAlignment="Center" TextAlignment="Center" Margin="20,20,20,0"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                <TextBlock Text="Système de réservation de salles intelligent"
                           FontSize="18" TextAlignment="Center" Margin="20,10,20,0" 
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                <TextBlock Text="Réservez facilement vos salles pour vos réunions et cours."
                           FontSize="16" TextAlignment="Center" Margin="20,20,20,30" 
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>

