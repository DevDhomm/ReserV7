<Page
  x:Class="Spacium.Views.Pages.BookRoomPage"
  x:Name="RootPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:Spacium.Views.Pages"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  Title="Réserver une Salle"
  d:DataContext="{d:DesignInstance local:BookRoomPage, IsDesignTimeCreatable=False}"
  d:DesignHeight="450"
  d:DesignWidth="1000"
  ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
  ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  UseLayoutRounding="True"
  SnapsToDevicePixels="True"
  TextOptions.TextFormattingMode="Display"
  TextOptions.TextRenderingMode="ClearType"
  mc:Ignorable="d"
>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <Border Grid.Row="0" Background="{DynamicResource ControlFillColorDefaultBrush}" Padding="20" Margin="0" BorderThickness="0,0,0,2" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}">
      <StackPanel Orientation="Vertical">
        <TextBlock FontSize="24" FontWeight="Bold" Text="Réserver une Salle" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,4" />
        <TextBlock FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Trouvez et réservez la salle que vous recherchez" />
      </StackPanel>
    </Border>

    <!-- Filters Section -->
    <Border Grid.Row="1" Background="{DynamicResource ControlFillColorDefaultBrush}" Margin="20,20,20,20" Padding="20" CornerRadius="12" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1,1,1,1">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="250" />
          <ColumnDefinition Width="150" />
          <ColumnDefinition Width="150" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Search by name -->
        <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,12,0">
          <TextBlock Text="Recherche par nom" FontSize="12" FontWeight="Bold" Margin="0,0,0,4" />
          <TextBox Text="{Binding ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <!-- Min Capacity -->
        <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,12,0">
          <TextBlock Text="Capacite minimale" FontSize="12" FontWeight="Bold" Margin="0,0,0,4" />
          <TextBox Text="{Binding ViewModel.MinCapacity, Mode=TwoWay}" />
        </StackPanel>

        <!-- Floor -->
        <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,12,0">
          <TextBlock Text="Étage" FontSize="12" FontWeight="Bold" Margin="0,0,0,4" />
          <ComboBox SelectedIndex="{Binding ViewModel.SelectedFloor, Mode=TwoWay}">
            <ComboBoxItem>Tous</ComboBoxItem>
            <ComboBoxItem>1</ComboBoxItem>
            <ComboBoxItem>2</ComboBoxItem>
            <ComboBoxItem>3</ComboBoxItem>
          </ComboBox>
        </StackPanel>

        <!-- Apply Filter Button -->
        <Button Grid.Column="4" Grid.Row="0" Command="{Binding ViewModel.ApplyFiltersCommand}" Cursor="Hand" Background="{DynamicResource BrandPrimaryBrush}" Foreground="White" Content="Appliquer les filtres" VerticalAlignment="Bottom" Margin="0,0,8,0" />

        <!-- Reset Button -->
        <Button Grid.Column="5" Grid.Row="0" Command="{Binding ViewModel.ResetFiltersCommand}" Content="Reset" Cursor="Hand" Background="IndianRed" VerticalAlignment="Bottom" />

        <!-- Equipment Filters -->
        <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="6" Margin="0,12,0,0">
          <TextBlock Text="Filtre par type d'équipement" FontSize="12" FontWeight="Bold" Margin="0,0,0,6" />
          <ItemsControl ItemsSource="{Binding ViewModel.EquipementFilters}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <WrapPanel Orientation="Horizontal" />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <CheckBox Content="{Binding Type}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="0,0,12,0" VerticalAlignment="Center" />
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </StackPanel>

        <!-- Selection info -->
        <TextBlock Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="6" Margin="0,8,0,0" FontSize="11"  Text="{Binding ViewModel.Salles.Count, StringFormat='Sélection: {0} salle(s) trouvée(s)'}" />
      </Grid>
    </Border>

    <!-- Rooms Grid -->
    <ScrollViewer x:Name="RoomsScrollViewer" Grid.Row="2" Margin="20,0,20,20" PreviewMouseWheel="RoomsScrollViewer_PreviewMouseWheel">
      <ItemsControl ItemsSource="{Binding ViewModel.Salles, Mode=OneWay}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource BrandAccentBrush}" BorderThickness="1.5" CornerRadius="10" Margin="0,0,16,16" Width="340" Padding="0">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="*" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Card Header -->
                <StackPanel Grid.Row="0" Margin="16,16,16,10">
                  <DockPanel LastChildFill="False" Margin="0,0,0,8">
                    <TextBlock DockPanel.Dock="Left" FontSize="18" FontWeight="Bold" Text="{Binding Nom}" TextWrapping="Wrap" VerticalAlignment="Center" />
                    <Border DockPanel.Dock="Right" Background="{DynamicResource ControlFillColorTertiaryBrush}" CornerRadius="12" Padding="10,4">
                      <TextBlock FontSize="11" FontWeight="SemiBold" Text="{Binding Type}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </Border>
                  </DockPanel>

                  <StackPanel Orientation="Horizontal">
                    <Border Background="{DynamicResource ControlFillColorSecondaryBrush}" CornerRadius="10" Padding="8,3" Margin="0,0,8,0">
                      <TextBlock FontSize="11" Text="{Binding Capacite, StringFormat='👥 {0} pers.'}" />
                    </Border>
                    <Border Background="{DynamicResource ControlFillColorSecondaryBrush}" CornerRadius="10" Padding="8,3">
                      <TextBlock FontSize="11" Text="{Binding Etage, StringFormat='🏢 Étage {0}'}" />
                    </Border>
                  </StackPanel>
                </StackPanel>

                <!-- Card Content - Description + Equipments -->
                <StackPanel Grid.Row="1" Margin="16,0,16,12">
                  <TextBlock Text="Description" FontSize="12" FontWeight="Bold" Margin="0,0,0,4" />
                  <TextBlock Text="{Binding Description}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" MaxHeight="48" Margin="0,0,0,10" />

                  <TextBlock FontSize="12" FontWeight="Bold" Text="Équipements" Margin="0,0,0,6" />
                  <ItemsControl ItemsSource="{Binding Equipements}" MaxHeight="84">
                    <ItemsControl.ItemsPanel>
                      <ItemsPanelTemplate>
                        <WrapPanel />
                      </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <Border Background="{DynamicResource ControlFillColorTertiaryBrush}" CornerRadius="10" Padding="8,3" Margin="0,0,8,8">
                          <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding Nom}" />
                        </Border>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>

                  <TextBlock Text="Aucun équipement renseigné" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Visibility="{Binding Equipements, Converter={StaticResource EmptyCollectionToVisibilityConverter}}" />
                </StackPanel>

                <!-- Card Footer Button -->
                <Button Grid.Row="2" Command="{Binding DataContext.ViewModel.SelectRoomForBookingCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Cursor="Hand" Background="{DynamicResource BrandPrimaryBrush}" Foreground="White" Content="Réserver cette salle" Margin="16,0,16,16" Padding="12,8,12,8" />
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</Page>


